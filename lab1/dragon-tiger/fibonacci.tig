let
    function fibonacci(n : int) : int =
        /* ... complete here ... */
        if n = 1 | n = 2 then
            1
        else
            fibonacci(n - 1) + fibonacci(n - 2)
in
    for i := 1 to 8 do
        (print_int(fibonacci(i)); print("\n"))
end